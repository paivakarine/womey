// import { CONFIG } from "../types";

const INITIAL_STATE = {
  token: "",
  user: {},
};

const reducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
       default:
      return state;
  }
};

export default reducer;
